# [OWNER] — This file configures Dart's static analyzer.
# [OWNER] — It catches bugs and enforces consistent coding style before runtime.
# [DEV] — This is a strict configuration suitable for production-grade projects.
# [DEV] — Consider adjusting rule severity based on team preferences.

include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

  errors:
    # [DEV] — Treat these as errors, not warnings
    missing_return: error
    dead_code: error
    unused_import: warning
    unused_local_variable: warning
    unnecessary_null_comparison: error
    invalid_annotation_target: ignore

  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "build/**"

linter:
  rules:
    # [DEV] — Style rules
    always_declare_return_types: true
    annotate_overrides: true
    avoid_empty_else: true
    avoid_print: true
    avoid_redundant_argument_values: true
    avoid_returning_null_for_future: true
    avoid_slow_async_io: true
    avoid_type_to_string: true
    avoid_unnecessary_containers: true
    cancel_subscriptions: true
    close_sinks: true
    directives_ordering: true
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_single_quotes: true
    require_trailing_commas: true
    sort_child_properties_last: true
    unawaited_futures: true
    unnecessary_await_in_return: true
    unnecessary_lambdas: true
    unnecessary_null_aware_assignments: true
    unnecessary_parenthesis: true
    unnecessary_string_escapes: true
    unnecessary_string_interpolations: true
    use_key_in_widget_constructors: true
    sized_box_for_whitespace: true
    use_full_hex_values_for_flutter_colors: true

    # [DEV] — Naming conventions
    camel_case_types: true
    camel_case_extensions: true
    non_constant_identifier_names: true
    constant_identifier_names: true
    file_names: true
    library_prefixes: true

    # [DEV] — Null safety strictness
    unnecessary_null_in_if_null_operators: true
    null_check_on_nullable_type_parameter: true
